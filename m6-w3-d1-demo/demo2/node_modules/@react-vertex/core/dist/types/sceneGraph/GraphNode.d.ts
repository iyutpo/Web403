import { mat4, vec3 } from 'gl-matrix';
import { SceneNode } from './SceneNode';
import { MatrixProps } from '../types';
export declare const isGraphNode: unique symbol;
export default class GraphNode {
    parent?: GraphNode;
    children: GraphNode[];
    matrix: mat4;
    worldMatrix: mat4;
    needsMatrixUpdate: boolean;
    userManagedMatrix: boolean;
    position?: vec3;
    rotation?: vec3;
    scale?: vec3;
    root?: SceneNode;
    constructor();
    [isGraphNode]: boolean;
    add(child: GraphNode): void;
    remove(child: GraphNode): void;
    updateWorldMatrix(): void;
    updateMatrix(): void;
    applyMatrixProps(nextprops: MatrixProps, prevProps: MatrixProps): void;
}
