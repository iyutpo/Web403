import { mat4 } from 'gl-matrix';
import GraphNode from './GraphNode';
import { CameraNodeProps } from '../types';
export declare const isCameraNode: unique symbol;
export declare class CameraNode extends GraphNode {
    constructor();
    [isCameraNode]: boolean;
    camera: null;
    view: mat4;
    projection: mat4;
    applyProps(nextprops: CameraNodeProps, prevProps?: CameraNodeProps): void;
}
