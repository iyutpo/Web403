import GraphNode from './GraphNode';
import { MatrixProps } from '../types';
export declare const isGroupNode: unique symbol;
export declare class GroupNode extends GraphNode {
    constructor();
    [isGroupNode]: boolean;
    applyProps(nextprops: MatrixProps, prevProps?: MatrixProps): void;
}
