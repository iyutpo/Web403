import { GLContext } from '../types';
export declare function useProgram(gl: GLContext, vertSource: string, fragSource: string): WebGLProgram;
export declare function useProgramUniforms(gl: GLContext, program: WebGLProgram): {
    [name: string]: WebGLUniformLocation | null;
};
