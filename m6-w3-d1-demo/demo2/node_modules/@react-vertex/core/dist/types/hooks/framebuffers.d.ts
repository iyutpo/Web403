import { GLContext, GetTextureOptions } from '../types';
export declare function useFramebuffer(gl: GLContext): WebGLFramebuffer | null;
export declare function useFBO(gl: GLContext, width: number, height: number, getTexOpts: GetTextureOptions): {
    tex: WebGLTexture | null;
    fbo: WebGLFramebuffer | null;
    attach(unit: number): number;
};
export declare function useDoubleFBO(gl: GLContext, width: number, height: number, getTexOpts: GetTextureOptions): {
    read: {
        tex: WebGLTexture | null;
        fbo: WebGLFramebuffer | null;
        attach(unit: number): number;
    };
    write: {
        tex: WebGLTexture | null;
        fbo: WebGLFramebuffer | null;
        attach(unit: number): number;
    };
    swap(): void;
};
