declare type PointLightInstance = {
    index: number;
};
export default class PointLights {
    instances: PointLightInstance[];
    diffuse: number[];
    position: number[];
    add(diffuse?: number[], position?: number[]): {
        index: number;
    };
    updateDiffuse({ index }: PointLightInstance, diffuse: number[]): void;
    updatePosition({ index }: PointLightInstance, position: number[]): void;
    remove({ index }: PointLightInstance): void;
}
export {};
