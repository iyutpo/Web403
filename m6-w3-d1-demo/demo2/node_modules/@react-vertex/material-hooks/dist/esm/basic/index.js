import { useProgram, useUniform2fv, useUniform3fv, useWebGLContext, useUniformSampler2d, usePointLightUniforms } from '@react-vertex/core';
import solidVert from './solid.vert';
import solidFrag from './solid.frag';
import texturedVert from './textured.vert';
import texturedFrag from './textured.frag';
var defaultKd = [1, 1, 1];
export function useBasicSolid() {
  var kd = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : defaultKd;
  var gl = useWebGLContext();
  var program = useProgram(gl, solidVert, solidFrag);
  useUniform3fv(gl, program, 'uKd', kd);
  return program;
}
var uVScale = [1.0, 1.0];
export function useBasicTextured(mapKd) {
  var gl = useWebGLContext();
  var program = useProgram(gl, texturedVert, texturedFrag);
  usePointLightUniforms(gl, program);
  useUniformSampler2d(gl, program, 'mapKd', mapKd);
  useUniform2fv(gl, program, 'uVScale', uVScale);
  return program;
}
//# sourceMappingURL=index.js.map