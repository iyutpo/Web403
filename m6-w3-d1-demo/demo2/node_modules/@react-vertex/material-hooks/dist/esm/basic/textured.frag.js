// @ts-ignore
export default "\n  precision <<FLOAT_PRECISION>> float;\n\n  uniform sampler2D mapKd;\n  uniform vec2 uVScale;\n\n  varying vec2 vUv;\n\n  const float gamma_648762903 = 2.2;\n\nfloat toLinear(float v) {\n  return pow(v, gamma_648762903);\n}\n\nvec2 toLinear(vec2 v) {\n  return pow(v, vec2(gamma_648762903));\n}\n\nvec3 toLinear(vec3 v) {\n  return pow(v, vec3(gamma_648762903));\n}\n\nvec4 toLinear(vec4 v) {\n  return vec4(toLinear(v.rgb), v.a);\n}\n\n  vec4 textureLinear(sampler2D uTex, vec2 uv) {\n    return toLinear(texture2D(uTex, uv));\n  }\n\n  void main() {\n    vec2 uv = vUv * uVScale;\n    vec3 kd = textureLinear(mapKd, uv).rgb;\n\n    gl_FragColor = vec4(kd, 1.0);\n  }\n";
//# sourceMappingURL=textured.frag.js.map