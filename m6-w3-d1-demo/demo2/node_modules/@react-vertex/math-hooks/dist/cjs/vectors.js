"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useVector2 = useVector2;
exports.useVector3 = useVector3;
exports.useVector4 = useVector4;

var _react = require("react");

var _glMatrix = require("gl-matrix");

function useVector2(x, y, configure) {
  var memoized = (0, _react.useMemo)(function () {
    var vector = _glMatrix.vec2.create();

    _glMatrix.vec2.set(vector, x, y);

    configure && configure(vector);
    return vector;
  }, [x, y]);
  return memoized;
}

function useVector3(x, y, z, configure) {
  var memoized = (0, _react.useMemo)(function () {
    var vector = _glMatrix.vec3.create();

    _glMatrix.vec3.set(vector, x, y, z);

    configure && configure(vector);
    return vector;
  }, [x, y, z]);
  return memoized;
}

function useVector4(x, y, z, w, configure) {
  var memoized = (0, _react.useMemo)(function () {
    var vector = _glMatrix.vec4.create();

    _glMatrix.vec4.set(vector, x, y, z, w);

    configure && configure(vector);
    return vector;
  }, [x, y, z, w]);
  return memoized;
}
//# sourceMappingURL=vectors.js.map